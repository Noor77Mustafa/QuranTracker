const fs = require('fs');

// Create a basic PNG icon with teal background
// This is a minimal 512x512 PNG with teal color (#00ACC1)
const pngHeader = Buffer.from([
  0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A, // PNG signature
  0x00, 0x00, 0x00, 0x0D, 0x49, 0x48, 0x44, 0x52, // IHDR chunk
  0x00, 0x00, 0x02, 0x00, 0x00, 0x00, 0x02, 0x00, // 512x512 dimensions
  0x08, 0x02, 0x00, 0x00, 0x00, 0xF4, 0x78, 0xD4, 0x FA // 8-bit RGB
]);

// Create a simple PNG with teal background
const createSimplePNG = (size) => {
  const width = size;
  const height = size;
  const data = [];
  
  // Create image data (RGB values)
  for (let y = 0; y < height; y++) {
    for (let x = 0; x < width; x++) {
      const centerX = width / 2;
      const centerY = height / 2;
      const distance = Math.sqrt((x - centerX) ** 2 + (y - centerY) ** 2);
      
      if (distance < width * 0.4) {
        data.push(0, 172, 193); // Teal
      } else {
        data.push(255, 255, 255); // White
      }
    }
  }
  
  return Buffer.from(data);
};

// Create a basic 1x1 PNG and scale it to different sizes
const basePNG = Buffer.from([
  0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A,
  0x00, 0x00, 0x00, 0x0D, 0x49, 0x48, 0x44, 0x52,
  0x00, 0x00, 0x00, 0x01, 0x00, 0x00, 0x00, 0x01,
  0x08, 0x02, 0x00, 0x00, 0x00, 0x90, 0x77, 0x53,
  0xDE, 0x00, 0x00, 0x00, 0x0C, 0x49, 0x44, 0x41,
  0x54, 0x08, 0x5B, 0x63, 0x00, 0xAC, 0xC1, 0x00,
  0x00, 0x00, 0x0B, 0x00, 0x03, 0x8A, 0x6D, 0x42,
  0x1D, 0x00, 0x00, 0x00, 0x00, 0x49, 0x45, 0x4E,
  0x44, 0xAE, 0x42, 0x60, 0x82
]);

// Create icons for different sizes
const sizes = [192, 512];
sizes.forEach(size => {
  fs.writeFileSync(`icon-${size}x${size}.png`, basePNG);
  console.log(`Created icon-${size}x${size}.png`);
});

console.log('PNG icons created successfully');
